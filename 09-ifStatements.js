/**
 * Strength in large ranges of information
 * 
 *  if (condition){}
 *  //block of code to be executed/ran if condition is true
 * }
 * 
 */

isOn = true;

// equality operatior ===
if (isOn){
    console.log("The light is on!");
}
// Does isOn have a value of True?
if (isOn){
    console.log("The light has been on for a while.");
}
// false === false) evaluates to true.
isOn = false;
if (isOn){
    no;
}
if (isOn === false){
    console.log("The light is off!")
}

// ! is the logical NOT operator, inverts the boolean value present in the current sting.
if (!isOn){
    console.log("The light is still off.")
}